import React from 'react';
import { ScreenContainer, Text } from '../../components';

const Contacts = () => (
  <ScreenContainer>
    <Text>Contacts</Text>
  </ScreenContainer>
);

export default Contacts;
